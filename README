Processing and Analysis of Geophysics And Numbers

Try it out:

# Install GDAL with python, spatiallite, and netcdf support
$ yum install libspatialite-devel

# Install
$ wget http://download.osgeo.org/gdal/gdal-1.9.0.tar.gz
$ tar zxvf gdal-1.9.0.tar.gz
$ cd gdal-1.9.0
$ ./configure; make
$ make install

# Install spatialindex 1.7.0+
$ wget http://download.osgeo.org/libspatialindex/spatialindex-src-1.7.1.tar.gz
$ tar zxvf spatialindex-src-1.7.1.tar.gz
$ cd spatialindex-src-1.7.1
$ ./configure; make
$ make install

# Install paegan library dependencies
$ pip install -r requirements.txt

# Run the paegan tests
$ ./run_tests.py